while read -r TAR_FILE REPOSITORY TAG; do

    [[ -z "$TAR_FILE" || "$TAR_FILE" == \#* ]] && continue

    # load → tag → push logic...

done < "$IMAGES_LIST"



LOAD_OUTPUT=$(docker load --input "$TAR_PATH" 2>&1)
log_info "  $LOAD_OUTPUT"

LOADED_IMAGE=$(echo "$LOAD_OUTPUT" | grep -oP '(?<=Loaded image: )\S+' | head -1)
 
if [[ -z "$LOADED_IMAGE" ]]; then
      LOADED_IMAGE="${REPOSITORY}:${TAG}"
      log_warn "  Could not auto-detect image name; using: $LOADED_IMAGE"
fi
