# Stop PostgreSQL server every Friday at 6 PM
0 18 * * 5 /path/to/postgres-weekend-manager.sh stop

# Start PostgreSQL server every Monday at 6 AM
0 6 * * 1 /path/to/postgres-weekend-manager.sh start

# Optional: Check status every day at noon
0 12 * * * /path/to/postgres-weekend-manager.sh status
