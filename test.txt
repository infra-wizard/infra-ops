version: "1"

project:
  name: "vip-rcip"
  branch: "main"  # Change to your current branch name

capture:
  coverity:
    buildCommands:
      - "cd vip/build"
      - "./build-debug.sh"
      - "make"
    cleanCommands:
      - "cd vip/build"
      - "make clean || echo 'Clean completed'"

serverUrl: "https://polaris.blackduck.com"
