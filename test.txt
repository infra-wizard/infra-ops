# Set environment variable for the executable location
    echo "VEHICLE_ARM_EXEC=$(pwd)/vehicle-arm0-version" >> $GITHUB_ENV
    
    # Add current directory to PATH
    echo "$(pwd)" >> $GITHUB_PATH
