az acr build \
  --registry lumen001wus3dev \
  --image data-analytics:latest \
  --file Dockerfile \
  .


# Run a temporary Ubuntu pod in the same namespace
kubectl run debug-pod --image=ubuntu:22.04 -n data-analytics --rm -it --restart=Never -- bash
